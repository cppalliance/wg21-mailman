
## Ansible customizations

When deploying the ansible role https://github.com/cppalliance/ansible-mailman3, copy the custom-wg21/ folder here to the ansible tasks/ folder, so the resulting directory structure is tasks/custom-wg21/main.yml.

Notice ../scripts/templates.sh. 
